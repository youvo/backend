name: OAuth Grant Remote
type: module
description: Pings the OAuth Relay for authentication.
package: Interfaces

core_version_requirement: ^10.3 || ^11

dependencies:
  - consumers:consumers
  - drupal:rest
  - simple_oauth:simple_oauth
  - youvo:youvo

configure: oauth_grant_remote.settings
