<h3>Hello, {{ page.field_name }}!</h3>

<p class="leader">
  {% if page.academy %}
    <a class="button button--primary" href="{{ path('entity.lecture.collection') }}">
      {{ 'Academy Administration'|t }}
    </a>
  {% endif %}
  {% if page.academy_log %}
    <a class="button button--primary" href="{{ path('academy_log.overview') }}">
      {{ 'Academy Log'|t }}
    </a>
  {% endif %}
</p>
